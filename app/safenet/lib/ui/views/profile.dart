import 'package:flutter/material.dart';

/// Page where the Details of the user are shown
class Profile extends StatelessWidget {
  // TODO: Currently the page is empty, must develop it to show the user's details after firebase integration.
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Profile'),
      ),
    );
  }
}
